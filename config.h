#define RENDER_USE_DELAY // Use delay when rendering instead of just returning for the next cycle
//#define CLOCK_TRIM_HOURS
#define EFFECT_SPEED 100
#define EFFECT_SLOT_MACHINE

#define ANTI_POISON_DELAY 200UL

